// Package plugins defines the extension points that will let IAMXFREE
// support new technologies (process supervisors, container runtimes,
// orchestrators) without modifying core packages. Empty for now; it exists
// so the boundary is reserved from the start of the project.
package plugins
