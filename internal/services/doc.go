// Package services holds business logic that coordinates one or more
// managers and repositories to fulfill a domain operation (e.g. deploying an
// application, rotating environment variables). Services are the boundary
// core depends on instead of talking to managers directly.
package services
