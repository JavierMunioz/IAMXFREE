// Package config loads, validates and exposes IAMXFREE's own configuration
// (paths, defaults, user preferences) independently of the configuration of
// the applications it manages.
package config
